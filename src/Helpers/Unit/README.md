# unit(value)

Convert a string or a number to a valid CSS unit.

@param {String|Number} The value to convert
@return {String}

    unit(100)
    // Outputs: '100px'

    unit('100%')
    // Outputs: '100%'
